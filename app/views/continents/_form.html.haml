= semantic_form_for @continent, :html => { :class => 'form-horizontal' } do |f|
  = f.input :title
  = f.submit nil, :class => 'btn btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), mounts_path, :class => 'btn'
